This folder is for debugging only. The projects in the Sbfn folder are excluded from build in the Configuration Manager for both release and debug builds.

The debugger will step into this code provided it is in sync with the version used to build the nugets.

Run the PS script ".\sbfn.ps1" to get the latest Sbfn source code.