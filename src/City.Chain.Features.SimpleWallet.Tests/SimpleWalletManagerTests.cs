using System;
using Xunit;

namespace City.Chain.Features.SimpleWallet.Tests
{
    public class SimpleWalletManagerTests
    {
        [Fact]
        public void CreateSimpleWallet()
        {
            //SimpleWalletHub hub = new SimpleWalletHub();

            //var connectionId = Guid.NewGuid().ToString();

            //hub.CreateWallet("citychain-mobile-0.0.1", connectionId);

            //// TODO: Consider adding some bloomfilter type of ability, to increase privacy with a method similar to "filterload" message.
            //hub.Watch("SMsZGWkF9zR5mjxWTYsDq9pU5ZHdUQw1jJ", connectionId);

            //hub.Remove(connectionId);
        }

        [Fact]
        public void CreateSimpleWalletAndWatch()
        {
            //SimpleWalletHub hub = new SimpleWalletHub();

            //var connectionId = Guid.NewGuid().ToString();

            //hub.CreateWallet("citychain-mobile-0.0.1", connectionId);

            //// TODO: Consider adding some bloomfilter type of ability, to increase privacy with a method similar to "filterload" message.
            //hub.Watch("SMsZGWkF9zR5mjxWTYsDq9pU5ZHdUQw1jJ", connectionId);

            //hub.Remove(connectionId);
        }
    }
}
